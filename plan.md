Refactor servicesStatus = servicesStatusNew into Service struct

Generate docs/index

Add service filtering to test

Add test level
    full deploy test or just sanity check

Add new service.yml and doc structure to tests

---
tinc: "{{ enable_tinc }}"
tor: "{{ enable_tor }}"
enabled_services:
  apache2: "{{ enable_apache2 }}"
  apple_health_influx: "{{ enable_apple_health_influx }}"
  airsonic: "{{ enable_airsonic }}"
  authelia: "{{ enable_authelia }}"
  beets: "{{ enable_beets }}"
  bitwarden: "{{ enable_bitwarden }}"
  bookstack: "{{ enable_bookstack }}"
  bulletnotes: "{{ enable_bulletnotes }}"
  calibre: "{{ enable_calibre }}"
  codeserver: "{{ enable_codeserver }}"
  darksky_influx: "{{ enable_darksky_influx }}"
  dasher: "{{ enable_dasher }}"
  digikam: "{{ enable_digikam }}"
  docs: "{{ enable_docs }}"
  duplicati: "{{ enable_duplicati }}"
  drone: "{{ enable_drone }}"
  emby: "{{ enable_emby }}"
  firefly_iii: "{{ enable_firefly_iii }}"
  freshrss: "{{ enable_freshrss }}"
  ghost: "{{ enable_ghost }}"
  gitea: "{{ enable_gitea }}"
  grafana: "{{ enable_grafana }}"
  grownetics: "{{ enable_grownetics }}"
  guacamole: "{{ enable_guacamole }}"
  healthchecks: "{{ enable_healthchecks }}"
  homeassistant: "{{ enable_homeassistant }}"
  homebridge: "{{ enable_homebridge }}"
  homedash: "{{ enable_homedash }}"
  inventario: "{{ enable_inventario }}"
  jackett: "{{ enable_jackett }}"
  jellyfin: "{{ enable_jellyfin }}"
  kibitzr: "{{ enable_kibitzr }}"
  lidarr: "{{ enable_lidarr }}"
  mailserver: "{{ enable_mailserver }}"
  mailu: "{{ enable_mailu }}"
  mashio: "{{ enable_mashio }}"
  matomo: "{{ enable_matomo }}"
  mayan: "{{ enable_mayan }}"
  miniflux: "{{ enable_miniflux }}"
  minio: "{{ enable_minio }}"
  monicahq: "{{ enable_monicahq }}"
  mstream: "{{ enable_mstream }}"
  mylar: "{{ enable_mylar }}"
  netdata: "{{ enable_netdata }}"
  nextcloud: "{{ enable_nextcloud }}"
  nzbget: "{{ enable_nzbget }}"
  ombi: "{{ enable_ombi }}"
  openldap: "{{ enable_openldap }}"
  openvpn: "{{ enable_openvpn }}"
  organizr: "{{ enable_organizr }}"
  ownphotos: "{{ enable_ownphotos }}"
  paperless: "{{ enable_paperless }}"
  pihole: "{{ enable_pihole }}"
  piwigo: "{{ enable_piwigo }}"
  pixelfed: "{{ enable_pixelfed }}"
  plex: "{{ enable_plex }}"
  poli: "{{ enable_poli }}"
  portainer: "{{ enable_portainer }}"
  privatebin: "{{ enable_privatebin }}"
  radarr: "{{ enable_radarr }}"
  restic: "{{ enable_restic }}"
  searx: "{{ enable_searx }}"
  sickchill: "{{ enable_sickchill }}"
  sonarr: "{{ enable_sonarr }}"
  speedtest: "{{ enable_speedtest }}"
  syncthing: "{{ enable_syncthing }}"
  tautulli: "{{ enable_tautulli }}"
  thelounge: "{{ enable_thelounge }}"
  tick: "{{ enable_tick }}"
  transmission: "{{ enable_transmission }}"
  trilium: "{{ enable_trilium }}"
  ubooquity: "{{ enable_ubooquity }}"
  wallabag: "{{ enable_wallabag }}"
  webvirtmgr: "{{ enable_webvirtmgr }}"
  wekan: "{{ enable_wekan }}"
  watchtower: "{{ enable_watchtower }}"
  xfinityusageinfluxdb: "{{ enable_xfinityusageinfluxdb }}"
  zulip: "{{ enable_zulip }}"
  minecraft: "{{ enable_minecraft }}"
  sabnzbd: "{{ enable_sabnzbd }}"
apache2: 
apple_health_influx: 
airsonic: 
authelia:
  duo: 
  default: 
  max: 
  domain: 
beets: 
bitwarden: 
bookstack: 
bulletnotes: 
calibre: 
codeserver: 
darksky_influx: 
dasher: 
digikam: 
docs: 
duplicati: 
drone: 
emby: 
firefly_iii: 
freshrss: 
ghost: 
gitea: 
grafana: 
grocy: 
grownetics: 
guacamole: 
healthchecks: 
homeassistant: 
homebridge: 
homedash: 
inventario: 
jackett: 
jellyfin: 
kibitzr: 
lidarr: 
mailserver: 
mashio: 
matomo: 
mayan: 
miniflux: 
minio: 
monicahq: 
mstream: 
mylar: 
netdata: 
nextcloud: 
nzbget: 
ombi: 
openldap: 
openvpn: 
organizr: 
ownphotos: 
paperless: 
pihole: 
piwigo: 
pixelfed: 
plex: 
poli: 
portainer: 
privatebin: 
radarr: 
restic: 
sickchill: 
sonarr: 
syncthing: 
tautulli: 
thelounge: 
tick: 
transmission: 
trilium: 
ubooquity: 
wallabag: 
webvirtmgr: 
wekan: 
watchtower: 
xfinityusageinfluxdb: 
zulip: 
speedtest: 
enable_tinc: false
vpn_ip: 10.0.0.1
netname: homelabos
vpn_interface: tun0
vpn_netmask: 255.255.0.0
vpn_subnet_cidr_netmask: 32
smtp: 
traefik:
  additional_env_vars: 
bastion:
  wireguard: 
  enabled: false
open_ldap:
  seed: 

# These are the defaults. They are overridden by values in your config.yml file.
common_timezone: Etc/UTC

domain: localhost
admin_email: test@test.com

enable_tinc: False
enable_tor: False
enable_nas: False

https_only: false

volumes_root: /var/homelabos

# Enabled List

enable_apple_health_influx: False
enable_airsonic: False
enable_bitwarden: False
enable_bookstack: False
enable_bulletnotes: False
enable_codeserver: False
enable_darksky_influx: False
enable_dasher: False
enable_docs: False
enable_emby: False
enable_firefly_iii: False
enable_ghost: False
enable_gitea: False
enable_grafana: False
enable_guacamole: False
enable_homeassistant: False
enable_homedash: False
enable_inventario: False
enable_jackett: False
enable_jellyfin: False
enable_kibitzr: False
enable_mailserver: False
enable_mashio: False
enable_matomo: False
enable_miniflux: False
enable_minio: False
enable_monicahq: False
enable_nextcloud: False
enable_nzbget: False
enable_openldap: False
enable_openvpn: False
enable_organizr: False
enable_paperless: False
enable_pihole: False
enable_plex: False
enable_portainer: False
enable_sonarr: False
enable_radarr: False
enable_restic: False
enable_syncthing: False
enable_thelounge: False
enable_tick: False
enable_transmission: False
enable_xfinityusageinfluxdb: False
enable_zulip: False

enabled_services:
  tinc: "{{ enable_tinc }}"
  tor: "{{ enable_tor }}"

  apple_health_influx: "{{ enable_apple_health_influx }}"
  airsonic: "{{ enable_airsonic }}"
  bitwarden: "{{ enable_bitwarden }}"
  bookstack: "{{ enable_bookstack }}"
  bulletnotes: "{{ enable_bulletnotes }}"
  codeserver: "{{ enable_codeserver }}"
  darksky_influx: "{{ enable_darksky_influx }}"
  dasher: "{{ enable_dasher }}"
  docs: "{{ enable_docs }}"
  emby: "{{ enable_emby }}"
  firefly_iii: "{{ enable_firefly_iii }}"
  ghost: "{{ enable_ghost }}"
  gitea: "{{ enable_gitea }}"
  grafana: "{{ enable_grafana }}"
  guacamole: "{{ enable_guacamole }}"
  homeassistant: "{{ enable_homeassistant }}"
  homedash: "{{ enable_homedash }}"
  inventario: "{{ enable_inventario }}"
  jackett: "{{ enable_jackett }}"
  jellyfin: "{{ enable_jellyfin }}"
  kibitzr: "{{ enable_kibitzr }}"
  mailserver: "{{ enable_mailserver }}"
  mashio: "{{ enable_mashio }}"
  matomo: "{{ enable_matomo }}"
  miniflux: "{{ enable_miniflux }}"
  minio: "{{ enable_minio }}"
  monicahq: "{{ enable_monicahq }}"
  nextcloud: "{{ enable_nextcloud }}"
  nzbget: "{{ enable_nzbget }}"
  openldap: "{{ enable_openldap }}"
  openvpn: "{{ enable_openvpn }}"
  organizr: "{{ enable_organizr }}"
  paperless: "{{ enable_paperless }}"
  pihole: "{{ enable_pihole }}"
  plex: "{{ enable_plex }}"
  portainer: "{{ enable_portainer }}"
  sonarr: "{{ enable_sonarr }}"
  radarr: "{{ enable_radarr }}"
  restic: "{{ enable_restic }}"
  syncthing: "{{ enable_syncthing }}"
  thelounge: "{{ enable_thelounge }}"
  tick: "{{ enable_tick }}"
  transmission: "{{ enable_transmission }}"
  xfinityusageinfluxdb: "{{ enable_xfinityusageinfluxdb }}"
  zulip: "{{ enable_zulip }}"

darksky_key:
latitude:
longitude:
openvpn_provider:
openvpn_username:
openvpn_password:
openvpn_config:
nas_path:
nas_user:
nas_pass:
nas_workgroup:
s3_access_key:
s3_secret_key:
s3_backup_password:
homeassistant_api_key:
apple_health_nextcloud_username:
xfinity_user:
xfinity_password:
monica_db_password:
smtp_host: false
smtp_port:
smtp_user:
smtp_pass:
smtp_from_email:
smtp_from_name:

#####################################################################################
#          Leave everything below as is, unless you know what you're doing          #
#####################################################################################

# Tinc Stuff
vpn_ip: 10.0.0.1
netname: homelabos
physical_ip: 8.8.8.8
vpn_interface: tun0
vpn_netmask: 255.255.0.0
vpn_subnet_cidr_netmask: 32
tincserver_ip:
tincserver_ssh_user:

# Backup Stuff
s3_path: s3:http://minio:9000/restic-backups/
s3_backup_cron: 0 4 * * *

ldap_org_name: My Family

# Service List
services:
  - airsonic
  - bitwarden
  - bookstack
  - bulletnotes
  - codeserver
  - docs
  - emby
  - firefly_iii
  - ghost
  - gitea
  - grafana
  - guacamole
  - homeassistant
  - homedash
  - inventario
  - jackett
  - jellyfin
  - mailserver
  - mashio
  - matomo
  - miniflux
  - minio
  - monicahq
  - nextcloud
  - openldap
  - openvpn
  - organizr
  - paperless
  - pihole
  - plex
  - portainer
  - sonarr
  - radarr
  - restic
  - syncthing
  - thelounge
  - tick
  - transmission
  - zulip

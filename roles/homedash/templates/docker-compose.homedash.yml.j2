---
version: '3'

networks:
  traefik_network:
    external:
      name: homelabos_traefik

services:
  homedash:
    image: gonzague/homedash
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /var/homelabos/homedash:/data
    environment:
      - SALT=ivPWITRH5KPJE5oQQAgxuiCgIxBez6MF6idGmasWTUjZzhEx3iAtnI9TSAo6HAevApRoujC5
    restart: unless-stopped
    networks:
      - traefik_network
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=homelabos_traefik"
      - "traefik.http.services.homedash.loadbalancer.server.scheme=http"
      - "traefik.http.services.homedash.loadbalancer.server.port=4567"
      - "traefik.http.routers.homedash-http.rule=Host(`homedash.{{ domain }}`)"
      - "traefik.http.routers.homedash-http.entrypoints=http"
      - "traefik.http.routers.homedash-http.middlewares=auth@file, redirect@file, customFrameHomelab@file"
      - "traefik.http.routers.homedash.rule=Host(`homedash.{{ domain }}`)"
      - "traefik.http.routers.homedash.entrypoints=https"
      - "traefik.http.routers.homedash.middlewares=auth@file, customFrameHomelab@file"
      - "traefik.http.routers.homedash.tls=true"
      - "traefik.http.routers.homedash.tls.domains[0].main={{ domain }}"
      - "traefik.http.routers.homedash.tls.domains[0].sans=*.{{ domain }}"
      - "traefik.http.routers.homedash.tls.certresolver=cloudflare-dns"
{% if enable_tor %}
      - "traefik.tor.frontend.rule=Host:homedash.{{ tor_domain }}"
      - "traefik.tor.protocol=http"
      - "traefik.tor.port=4567"
{% endif %}

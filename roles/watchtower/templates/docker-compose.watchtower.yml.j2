      - "traefik.enable=true"
      - "traefik.docker.network=homelabos_traefik"
      - "traefik.http.services.watchtower.loadbalancer.server.scheme=http"
      - "traefik.http.services.watchtower.loadbalancer.server.port=0"
      - "traefik.http.routers.watchtower-http.rule=Host(`{{ services.watchtower.subdomain }}.{{ domain }}`)"
      - "traefik.http.routers.watchtower-http.entrypoints=http"
{% if not services.watchtower.https_only %}
      - "traefik.http.routers.watchtower-http.middlewares={% if services.watchtower.https_only %}redirect@file, {% else %}{% if services.watchtower.auth %}{% if services.authelia.enabled %}authelia@file{% else %}basicAuth@file{% endif %}, {% endif %}{% endif %}customFrameHomelab@file"
{% else %}
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "traefik.http.routers.watchtower-http.middlewares=redirect-to-https"
{% endif %}
      - "traefik.http.routers.watchtower.rule=Host(`{{ services.watchtower.subdomain }}.{{ domain }}`)"
      - "traefik.http.routers.watchtower.entrypoints=https"
      - "traefik.http.routers.watchtower.middlewares={% if services.watchtower.auth %}{% if services.authelia.enabled %}authelia@file{% else %}basicAuth@file{% endif %}, {% endif %}customFrameHomelab@file"
      - "traefik.http.routers.watchtower.tls=true"
---
version: '3'

services:
  watchtower:
    image: v2tec/watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - "{{ volumes_root }}/watchtower/config.json:/config.json"
    command: --interval 900 #15 minutes

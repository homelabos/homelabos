---
version: '3'

networks:
  traefik_network:
    external:
      name: homelabos_traefik

services:
  # Docker Control
  portainer:
    image: portainer/portainer
    restart: unless-stopped
    networks:
      - traefik_network
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /var/homelabos/portainer:/data
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=homelabos_traefik"
      - "traefik.http.services.portainer.loadbalancer.server.scheme=http"
      - "traefik.http.services.portainer.loadbalancer.server.port=9000"
      - "traefik.http.routers.portainer-http.rule=Host(`portainer.{{ domain }}`)"
      - "traefik.http.routers.portainer-http.entrypoints=http"
      - "traefik.http.routers.portainer-http.middlewares=auth@file, redirect@file, customFrameHomelab@file"
      - "traefik.http.routers.portainer.rule=Host(`portainer.{{ domain }}`)"
      - "traefik.http.routers.portainer.entrypoints=https"
      - "traefik.http.routers.portainer.middlewares=auth@file, customFrameHomelab@file"
      - "traefik.http.routers.portainer.tls=true"
      - "traefik.http.routers.portainer.tls.domains[0].main={{ domain }}"
      - "traefik.http.routers.portainer.tls.domains[0].sans=*.{{ domain }}"
      - "traefik.http.routers.portainer.tls.certresolver=cloudflare-dns"
{% if enable_tor %}
      - "traefik.tor.frontend.rule=Host:portainer.{{ tor_domain }}"
      - "traefik.tor.protocol=http"
      - "traefik.tor.port=9000"
{% endif %}

---
version: '3'

services:
  duckdns:
    image: linuxserver/duckdns 
    restart: unless-stopped
    environment:
      - TZ={{ common_timezone }}
      - SUBDOMAINS={{ domain }}
      - TOKEN={{duckdns.token}}

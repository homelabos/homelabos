---
version: '3'

# This networks section required for the package to be visible to Traefik.
# DO NOT Remove
networks:
  traefik_network:
    external:
      name: homelabos_traefik

services:
  url:
    image: draganczukp/simply-shorten:{{ simply-shorten.version }}
    environment:
      username: {{ default_username }}
      password: {{ default_password }}
    volumes:
      - "{{ volumes_root }}/simply-shorten/urls.sqlite:/urls.sqlite"
    restart: unless-stopped
    networks:
      - traefik_network
    labels:
      - "traefik.http.services.simply-shorten.loadbalancer.server.scheme=http"
      - "traefik.http.services.simply-shorten.loadbalancer.server.port=4567"
{% include './labels.j2' %}
